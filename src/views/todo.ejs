<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Détail Todo</title>
</head>
<body>
  <h1>Détails du ToDo</h1>
    <form action="/todos/<%= todo.id %>/update" method="post">
      <a href="../todos/">ALL TODOS</a>
      <p><strong>ID :</strong> <%= todo.id %></p>
      <input type="text" name="todo_title" value="<%= todo.title %>" required><br>
      <input type="text" name="todo_desc" value="<%= todo.description%>" required><br>
      <input type="date" value="<%= todo.created_at.toISOString().slice(0, 10) %>" readonly><br>
      <input type="date" name="due_time" value="<%= todo.due_time.toISOString().slice(0, 10) %>"><br>
      <p><strong>Statut :</strong> <%= todo.status %></p>

      <button type="submit">update todo</button>
    </form>
    <form action="/todos/<%= todo.id %>/delete" method="post">
      <button type="submit" onclick="return confirm('do you want to delte <%= todo.title %>')">Delete todo</button>
    </form>
</body>
</html>
